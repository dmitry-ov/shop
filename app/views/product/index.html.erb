<% @products.each do |p| %>
     
    <%= p.foto  %> 
    <%= p.name  %> </br>
    <%= p.price %> </br>
    <%= p.vendor %> </br> 
    <%= p.describe %> </br> 
    <%= p.category_id %> </br> 
    
  
<% end %>
